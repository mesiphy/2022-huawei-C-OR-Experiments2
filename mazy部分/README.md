# 2022-huawei-C-OR-Experiments2
高级运筹学实验2小组工作文件

随便写的（后面改）：

​	validation.py 使用gurobi 模块求解模型，适用于小型数据集验证模型结果，并与其他启发算法对比验证启发式算法的结果是否合理

​	data_preparation.py 是数据加载模块

​	optimize_sa.py 是使用模拟退火算法写的，没什么特别的

​	save_results.py 是保存求解结果csv文件到对应路径

​	visualize_results.py 是试图将最终结果可视化的文件，尚未完成。

​	generation_animation.py 也是，还没完成

​	result11.xlsx 是保存的求解结果，用于辅助可视化，也没有完善。

​	其他没有介绍的后面写。
